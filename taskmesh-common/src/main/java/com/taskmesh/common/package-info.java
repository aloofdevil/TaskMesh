/**
 * Shared types for TaskMesh: DTOs, enums, and event schemas used by both the
 * {@code taskmesh-control-plane} and {@code taskmesh-worker} modules, so the
 * wire contract between them is defined once instead of duplicated.
 * <p>
 * Empty as of Day 1; populated starting Day 2 (job/worker DTOs) and Day 3+
 * (claim/heartbeat request-response types, lifecycle event payloads).
 */
package com.taskmesh.common;
